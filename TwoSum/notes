TIME_COMPLEXITY : O(n)
SPACE_COMPLEXITY : O(n)
APPROACH : I used a HashMap to store visited elements and checked whether the complement (target − current) already exists, allowing me to solve the problem in O(n) time with O(n) extra space
