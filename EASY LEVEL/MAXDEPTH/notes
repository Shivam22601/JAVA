TIME_COMPLEXITY = O(n)
SPACE_COMPLEXITY = O(h)
APPROACH: We use recursive DFS (Post-order). Go all the way down to the leaves first, then on the way back up — at each node, take the max of left and right depth and add 1 (for the current node). Base case: root == null returns 0.