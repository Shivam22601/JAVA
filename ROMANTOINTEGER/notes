TIME_COMPLEXITY = O(n)
SPACE_COMPLEXITY = O(1)
APPROACH : String ko left se right traverse karo aur agar current Roman value next value se chhoti ho to subtract karo, warna add karke total sum return karo.